/**
 * Releases Layout
 *
 * Wrapper layout for the releases section of an app.
 * Provides consistent structure for releases pages.
 */

export default function ReleasesLayout({
    children,
}: {
    children: React.ReactNode
}) {
    return <>{children}</>
}
