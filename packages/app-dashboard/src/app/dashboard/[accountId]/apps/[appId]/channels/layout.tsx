/**
 * Channels Layout
 *
 * Layout wrapper for channels pages within an app.
 */

export default function ChannelsLayout({
    children,
}: {
    children: React.ReactNode
}) {
    return <>{children}</>
}
