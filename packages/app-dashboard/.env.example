# =============================================================================
# App Dashboard Environment Variables (packages/app-dashboard)
# =============================================================================
# Copy to .env.local and fill in values

# App URL
NEXT_PUBLIC_APP_URL=http://localhost:3001

# API URL
NEXT_PUBLIC_API_URL=https://api.bundlenudge.com

# =============================================================================
# DATABASE (Neon PostgreSQL for Better Auth)
# =============================================================================

# Get connection string from: https://console.neon.tech
DATABASE_URL=postgresql://user:pass@host/bundlenudge_app_auth

# =============================================================================
# AUTHENTICATION (Better Auth)
# =============================================================================

# Session signing secret (generate: openssl rand -hex 32)
BETTER_AUTH_SECRET=your-secret-here-min-32-chars

# =============================================================================
# OAUTH PROVIDERS
# =============================================================================

# GitHub OAuth - Create at: https://github.com/settings/applications/new
# Callback URL: http://localhost:3001/api/auth/callback/github (dev)
# Callback URL: https://app.bundlenudge.com/api/auth/callback/github (prod)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Google OAuth (optional) - Create at: https://console.cloud.google.com/apis/credentials
# Callback URL: http://localhost:3001/api/auth/callback/google (dev)
# Callback URL: https://app.bundlenudge.com/api/auth/callback/google (prod)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# =============================================================================
# DEVELOPMENT
# =============================================================================

# Development URLs (uncomment if needed)
# NEXT_PUBLIC_API_URL=http://localhost:8787
